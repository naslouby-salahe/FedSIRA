# Graphify audit

Graphify was queried against the repository graph before source review. The query surfaced the expected domain, experiment, evaluation, and runtime communities, including `src/fedsira/domain/types.py` and `src/fedsira/domain/enums.py`.

The final Graphify update contains 4,307 nodes and 30,456 edges. It reflects the focused `reproduction_progression` and `figure_observations` modules; no production call path is unexplained.
